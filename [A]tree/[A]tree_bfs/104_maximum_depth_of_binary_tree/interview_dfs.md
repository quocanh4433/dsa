> I use DFS with recursion, for each node, i recursive

> I recursively calculate ```(tính toán theo phương pháp đệ quy)``` the maximum depth of its left and right subtrees.

> The depth of the current node is the larger of those two depths plus one for the current node itself.

> The base case is when the node is null, where the depth is zero.